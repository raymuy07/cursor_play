---
globs: .py
alwaysApply: false
---
- Use fewer comments
- Follow PEP 8 style guidelines
- Use type hints where applicable
- Prefer SQLite for data persistence
- Call `setup_logging()` once at entry point; use `logger = logging.getLogger(__name__)` at module level
